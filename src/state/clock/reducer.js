
const initialState = {
  caption: [
    'A New Life of Crime',
    '#Theoldlifewascrimetoo',
  ],
  ticks: 2
};

export default reducer;

function reducer(state = initialState, action) {
  return state;
}
